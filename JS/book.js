class Book {
    name;
    author;
    price
}

class BookController {
    sortBooks(order) {
        // Sort in decending order of price
    }
}

class BookView {
    printBooksInAscendingOrderOfPrice() {
        // Print the book details in ascending order
    }

    printBooksInDescendingOrderOfPrice() {
        // Print the book details in descending order
    }
}

